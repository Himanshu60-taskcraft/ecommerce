<%= form_with(model: shopkeeper_category, local: true) do |form| %>
  <% if shopkeeper_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shopkeeper_category.errors.count, "error") %> prohibited this shopkeeper_category from being saved:</h2>

      <ul>
        <% shopkeeper_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
