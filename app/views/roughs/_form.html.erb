<%= form_with(model: rough, local: true) do |form| %>
  <% if rough.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rough.errors.count, "error") %> prohibited this rough from being saved:</h2>

      <ul>
        <% rough.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
